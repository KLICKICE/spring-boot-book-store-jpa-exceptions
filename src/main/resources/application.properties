spring:
datasource:
url: jdbc:mysql://${MYSQLDB_HOST:localhost}:${MYSQLDB_LOCAL_PORT:3306}/${MYSQLDB_DATABASE:bookstore}
username: ${MYSQLDB_USER:root}
password: ${MYSQLDB_ROOT_PASSWORD:SunlessGrey0706}
driver-class-name: com.mysql.cj.jdbc.Driver
spring.datasource.username=${MYSQLDB_USER:root}
spring.datasource.password=${MYSQLDB_ROOT_PASSWORD:SunlessGrey0706}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

spring.jackson.deserialization.fail-on-unknown-properties=true
server.servlet.context-path=/api
spring.liquibase.enabled=true

jwt.secret=${JWT_SECRET:supersecretkeysupersecretkeysupersecretkey}
jwt.expiration=${JWT_EXPIRATION:3600000}
